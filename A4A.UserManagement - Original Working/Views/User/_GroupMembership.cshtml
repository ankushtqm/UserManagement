<div class="GroupMembership" id="UserGroupMembership">
    <fieldset>
        <Legend class="niceLabel-bold"></Legend>
        <div class="text-danger niceLabel  bold " data-bind="html: ErrorMessageGM"></div><br />
        <div class="text-success niceLabel bold" data-bind="html: InfoMessageGM"></div>
        <br />
        <span class="niceLabel text-warning">User Groups: </span>
        <p>
            <input type="text" class="chosen form-control" style="width:300px;" id="name-search" data-bind="ko_autocomplete: { source: GetAllGroups, select: SelectGroupforUser ,minLength: 3 }" />
            <select id="inpGroupRole" class="form-control"
                    data-bind="options: Roles, optionsText: 'name', optionsValue: 'value', value:role"></select>
            <button class="z-btn  btn btn-primary" data-bind='click: AddGroup, enable: Roles().length > 0' type='submit'>Go</button> 
        </p>
        <table class="table" data-bind='visible: $data.UserGroups().length > 0'>
            <thead>
                <tr>
                    <th class="control-label">Name</th>
                    <th class="control-label">Role</th>
                </tr>
            </thead>
            <tbody data-bind="foreach: $data.UserGroups()">
                <tr>
                    <td><span data-bind='text: GroupName'></span> <span data-bind='text: GroupType'></span></td>
                    <td><span data-bind='text: Role'></span></td>
                    <td><a href='#' data-bind='click: $parent.removeUserGroup,visible:IsCurrentUserAdmin '>Delete</a></td>
                </tr>
            </tbody>
        </table>
    </fieldset>
</div>
 